sap.ui.define([
    "sap/ui/core/mvc/Controller"
], function (Controller) {
    "use strict";

    return Controller.extend("project1.controller.CreateProduct", {

        onInit: function () {
            // 초기화 시 동작 없음 (임시 화면용)
        }

    });
});
